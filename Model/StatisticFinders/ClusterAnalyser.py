class ClusterAnalyser():

    def __init__(self, nbaStatAnalyser):
        self.nbaStatAnalyser = nbaStatAnalyser

#We want this to take in all relevant stats and give us a players average distance from the computed mean
    def averageAwayFromMean(self, stats_list):

        player_stat_list_length = len(stats_list)
        player_stats_to_evaluate = len(stats_list[1][0])
        for i in stats_list:
            last

            print(i[0])


        #average of all individual stats, then an average of the average.

        return

    def stealToTurnoverComparisionOfTwoPlayers(self, playersA, playerB):
        return









